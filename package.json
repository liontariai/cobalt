{
    "name": "@liontari.ai/cobalt",
    "license": "SSPL-1.0",
    "scripts": {
        "format": "prettier --write .",
        "publish:all": "cd packages/runtime && bun publish && cd ../generate && bun publish && cd ../auth && bun publish && cd ../dev && bun publish && cd .."
    },
    "devDependencies": {
        "@types/bun": "latest",
        "prettier": "latest",
        "typescript": "~5.7.3"
    },
    "prettier": {
        "tabWidth": 4
    },
    "workspaces": [
        "packages/**/*",
        "examples/**/*",
        "tests/**/*"
    ]
}
